<navigation:Page 
    x:Class="Stackanimate.Home"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:navigation="clr-namespace:System.Windows.Controls;assembly=System.Windows.Controls.Navigation"
    xmlns:sdk="http://schemas.microsoft.com/winfx/2006/xaml/presentation/sdk" 
	mc:Ignorable="d" 
    Title="Home"
    Style="{StaticResource PageStyle}">
    <StackPanel x:Name="LayoutRoot">
        <TextBox Width="900" Text="{Binding Path=InputString, Mode=TwoWay}" />
        <Button Content="Get Question" Command="{Binding ParseQuestionIdCommand}" />
        <TextBox Width="100" Text="{Binding Path=OutputString, Mode=TwoWay}" />
        <Slider Width="200" />
    </StackPanel>
</navigation:Page>